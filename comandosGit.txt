
-----------iniciar sesion
$ git config --global user.email "correo electronico"
$ git config --global user.name "nombre de usuario"

git 

git clone https://github.com/PaolaGalindoCante/SIGPI_V1.3.git  --crear carpeta

cd nombrecarpeta -- entrar en el lugar de trabajo

git pull -- descargar trabajo



--Agregar archivos
git add -A  -- agregar archivo
git commit -m "mensaje" -- mensaje de lo que hicieron
git push --sube al repositorio


